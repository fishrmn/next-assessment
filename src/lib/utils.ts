// One class-name helper for the whole app: the same `cn` the shadcn components import.
// It joins conditional classes and resolves Tailwind conflicts ("p-2 p-4" becomes "p-4").
export { cn } from "cn"
