# Copy to .env.local (never committed) and fill in.

# Key for the Vercel AI Gateway, which serves the Blueprint agent's model.
AI_GATEWAY_API_KEY=

# Optional. Any Gateway model id that supports tool calls. Default: openai/gpt-5.6-luna
BLUEPRINT_AGENT_MODEL=
